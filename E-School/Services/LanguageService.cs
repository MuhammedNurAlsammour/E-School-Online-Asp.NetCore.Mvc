using System.Security.Cryptography.X509Certificates;

namespace EduSamaV4.Services;

public class LanguageService
{
    //public LanguageService()
    //{

    //}

    //private IServiceProvider _provider;
    //private IConfiguration _configuration;
    ////private EduContext _db;
    //public LanguageService(IServiceProvider provider, IConfiguration configuration)
    //{
    //    _provider = provider;
    //    _configuration = configuration;
    //    //_db = (EduContext)_provider.GetService(typeof(EduContext));
    //}
    //public string Session() => SessionID;

    //public string SessionID { get; set; }

    public string Session()
    {
        return "35";
    }
}